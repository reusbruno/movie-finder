---
name: build-log
description: Project build history and status for this repo - what's shipped, how each feature was verified, and known follow-ups/limitations. Use when asked about project status, what's left to ship, the build plan, the roadmap, or known issues/limitations in this codebase.
---

## Build plan

Phase 1 (done): core browsing experience.

1. ~~Server-side TMDB client + API routes~~ — done (`src/lib/tmdb.ts`, `src/app/api/movies/search`, `src/app/api/movies/[id]`). Verified against the live TMDB API; env-var handling checked clean (no raw token anywhere outside `.env.local`).
2. ~~Movies/Series toggle + shared UI shell~~ — done (layout/nav common to both modes).
3. ~~Search feature + recommendations grid~~ — done.
4. ~~Browse mode: genre checkboxes + sort controls~~ — done.
5. ~~IMDb/RT score filtering via OMDb~~ — done (`src/lib/ratings.ts`); see Known follow-ups for a pending dedup check.
6. ~~TMDB attribution notice in the footer~~ — done (`src/components/tmdb-attribution.tsx`).

Phase 2 (done): all six items shipped, each with its own verify-then-commit cycle.

1. ~~Free-text mood search~~ — built (`src/lib/mood-search.ts`, `src/app/api/movies/mood-search`, `src/app/api/tv/mood-search`). Claude Haiku 4.5 via structured outputs turns a query into genres (constrained to the real TMDB genre list)/keywords/reference titles/sort/year-range, resolved against TMDB (`with_keywords` added to `discoverMovies`/`discoverTV`) and fed into the existing discover pipeline. Degrades to a visible-but-disabled input ("Mood search — coming soon") when `ANTHROPIC_API_KEY` is unset; verified via lint/typecheck/build plus a real dev-server run (disabled state screenshotted, availability/validation/error-mapping branches exercised with a fake key against the live Anthropic API). Verified against the real Anthropic API on 2026-07-10 with `ANTHROPIC_API_KEY` finally added: "slow melancholic sci-fi" → sensible genres/keywords, "Interpreted as: …" caption renders. That real run surfaced a genuine bug: TMDB's `with_genres` was always OR'd (`|`), so a mood with 2+ genres (e.g. "Science Fiction, Drama") let pure-Drama titles with no Sci-Fi at all (Parasite, City of God, Mildred Pierce) into the results. Fixed by adding `genreMatchMode: "any" | "all"` to `discoverMovies`/`discoverTV` (`src/lib/tmdb.ts`) — the browse page's genre checkboxes keep `"any"` (OR, the standard multi-select convention), mood search now passes `"all"` (AND, genres are a hard filter for a mood). Also capped mood search to at most 2 genres (`MAX_GENRES_FROM_QUERY`) so an AND of 3+ loosely-related genres can't intersect to zero results, and the "Interpreted as: …" caption now derives from the same capped/resolved genre list actually used in the filter, not the raw uncapped LLM output. Re-verified live: every result for "slow melancholic sci-fi" now genuinely has both Science Fiction and Drama; a single-genre query ("funny heist movie" → Comedy, Crime) behaves as an AND of two and returns exactly what you'd expect; TV mood search checked too. Client-side mutual-exclusion UI (mood search clearing quick-search/genre/sort state and vice versa) still not separately verified. Two more real-usage fixes on 2026-07-10: (1) while a mood/blend/discover/search request was in flight, `MovieGrid` briefly showed its permanent "No movies found." message (the results state was still `null`, and `items = results ?? []` collapsed "no response yet" with "response was empty") - fixed generically in `MediaExplorer` (`resultsPending`, `src/components/skeletons.tsx`'s `SkeletonGrid`) since all four non-popular modes shared the exact same bug, not just mood search. (2) The AND-genre fix above still under-delivered for "cozy feel-good comedy for a rainy day": genre resolved to Comedy+Family AND'd, and TMDB's keyword tagging for abstract tone words ("cozy", "wholesome", "feel-good") turned out to be extremely sparse - measured directly against the live API, individual mood keywords covered as few as 5-26 movies in TMDB's *entire* catalog, so Comedy+Family AND'd with any of 5 such keywords collapsed to ~5 results (vs. ~1,746 for the same two genres with no keyword filter at all). Fixed in two parts: `discoverWithMoodFallback` (`mood-search.ts`, replacing the earlier `discoverWithGenreFallback`) now cascades keywords-dropped-first, genre-narrowed-to-primary-only as a last resort, since keyword tagging is the far sparser signal; and the `genres` field's LLM prompt now explicitly warns that TMDB's "Family" means children's content, not colloquial "wholesome", so it should only be picked when a query actually signals kids/family viewing - dropping keywords alone fixed the *count* but not the *flavor* (Comedy+Family sorted by popularity still surfaced Toy Story/Minions/Shrek), since "Family" itself was pulling toward children's content independent of keywords. The "Interpreted as: …" caption (both genre and keyword terms) reflects whichever filter set actually ended up applied. Verified live over several real LLM calls: "cozy feel-good comedy" no longer resolves to Family, typically landing on Comedy+Drama with 20 well-fitting results (Devil Wears Prada, The Intouchables, Forrest Gump; TV: The Bear, Shameless); an explicit "family movie night with the kids" query still correctly resolves to Family+Comedy (no over-correction); "slow melancholic sci-fi" (already had enough results) is unaffected; a 2s-delayed mood request confirmed no "No movies found" flash and a skeleton grid instead. See Known follow-ups for a `sort_by=popularity.desc` ranking-tone issue this pass didn't try to fix. One more real-usage bug on 2026-07-10: "modern romance" returned only 3 results including Forrest Gump (1994) and Indiscreet (1958) - the opposite of "modern". Root cause: `interpretMoodQuery`'s system prompt never told the model what today's date is, so it had no anchor to compute what "modern" should mean and fell back to treating it as a vibe keyword ("contemporary", "urban") instead of a year-range constraint - confirmed by inspecting three live runs, all of which returned titles spanning 1934-2026 with zero temporal narrowing regardless of the keyword-sparsity fallback above. Fixed by adding `` `Today's date is ${today}.` `` (computed server-side per request, not hardcoded) to the system prompt, and rewriting the `yearRange` schema's `gte`/`lte` descriptions to explicitly map fuzzy temporal language against that anchor - "modern"/"recent"/"contemporary" to roughly the last 10-15 years, "classic"/decade mentions to the right historical bounds - not just literal years. Also surfaced `yearRange` in the "Interpreted as: …" caption for the first time (`formatYearRange` in `media-explorer.tsx`) - it was being resolved and applied in the discover call already but was invisible in the UI and omitted from the API response's `interpretation`, which is exactly what made this bug hard to spot without querying the API directly. Verified live: "modern romance" now resolves `yearRange: {gte: 2011}` consistently and every result is from the last ~15 years; "recent thriller" → `gte: 2016`; "classic film noir" → `{gte: 1940, lte: 1958}` with results genuinely from that era (Touch of Evil, The Killing); "80s action movies" → exact `{gte: 1980, lte: 1989}` (Terminator, Scarface, Top Gun); caption confirmed rendering "1940–1959" etc. in a live browser session. One more threshold tuning pass the same day: "slow melancholic sci-fi" was still landing on exactly 3-5 results even after the keyword-drop fallback existed - measured directly against the live API, its 5 mood keywords OR'd, AND'd with Science Fiction+Drama, resolve to *exactly* 5 results, which satisfied `discoverWithMoodFallback`'s `MIN_MOOD_RESULTS = 5` threshold and never triggered the keyword-drop relaxation, even though 5 is clearly too thin - the same boundary trap as the "cozy" case's earlier borderline runs. Confirmed the fallback's relaxation direction itself was already correct (dropping keywords for this genre pair yields 291-1,687 results depending on vote-count threshold) - only the "is this good enough to stop relaxing" bar was too low. Also worth noting for future tuning: a "drop to fewer keywords" middle step (as opposed to all keywords or none) wouldn't help here even in principle - `with_keywords` is OR'd, so the union of any subset of keywords is always ⊆ the union of all of them; there's no keyword-count step between "all 5" and "none" that could ever yield *more* than the full-5 count already did. Fixed by raising `MIN_MOOD_RESULTS` from 5 to 10. Verified live over 4 repeated runs: "slow melancholic sci-fi" now consistently drops keywords and returns 20 results, genre still AND'd on Science Fiction+Drama, every title still genuinely fitting the tone even without the keyword requirement (Interstellar, Blade Runner 2049, The Prestige, Arrival, Stalker, Metropolis) - the genre pair itself already captures most of the "slow, contemplative" signal. "Cozy feel-good comedy for a rainy day" re-checked for regression: still 20 results, still no Family genre, across 3 runs.
2. ~~Vibe blending~~ — built (`src/lib/vibe-blend.ts`, `src/app/api/movies/vibe-blend`, `src/app/api/tv/vibe-blend`, `src/components/title-picker.tsx`). Union of both titles' genres/keywords is the discover candidate pool (one page, 20 results); each candidate is scored per-signal (genre/keyword/recommendation-list membership), doubled-to-quadrupled when it hits *both* seed titles rather than one, and TMDB's own `/recommendations` overlap is weighted highest. New "Blend" mode in `MediaExplorer`, mutually exclusive with search/mood/filters, with a "Blending: X + Y" caption. Verified via lint/typecheck/build plus real TMDB calls (not mocked) through both the raw API and a driven browser session: Inception + Arrival → Interstellar top result; Breaking Bad + Better Call Saul → Dexter/The Sopranos; same-id, invalid-id, and missing-param cases all return clean errors. See Known follow-ups for the one thing still worth tuning.
3. ~~"Why this match" explanations~~ — built (`src/lib/match-explanation.ts`, `src/lib/keywords.ts`, `src/lib/anthropic-client.ts`, `src/lib/explain-match.ts`, `src/app/api/explain-match`). Deterministic explanation (no LLM) on every blend/mood-search/detail-page-recommendation card, reusing blend's genre/keyword scoring weights so ranking and explanation always agree; capped at 2 signals, "both seed titles" beats "one" on a tie, hidden entirely when nothing matched. New shared, cached keyword lookup (`src/lib/keywords.ts`, same TTL'd-Map shape as `ratings.ts`, no disk persistence) feeds blend/mood-search/detail-page recommendations alike. Optional on-demand Haiku elaboration via a new "Explain more" button (only rendered when `ANTHROPIC_API_KEY` is set), same shared Anthropic client now used by mood search too (extracted to `anthropic-client.ts`). Verified with real TMDB data — Interstellar blending Inception+Arrival produces the exact wording signed off on ("Shares Science Fiction with both Inception and Arrival — also spacecraft (Arrival).") — and a driven browser session confirming: plain browse/popular cards show no badge at all; blend/detail-page cards show the explanation; "Explain more" makes a real (non-mocked) call to Anthropic and degrades to "Try again" with the error in a title tooltip on failure, without navigating away from the grid. See Known follow-ups for what's pending the real API key.
4. ~~Where-to-watch~~ — built (`src/lib/watch-providers.ts`, `src/components/watch-providers.tsx`). New `getWatchProviders(mediaType, id)` in `tmdb.ts` hits `/movie|tv/{id}/watch/providers`; wrapped in `watch-providers.ts`, same TTL'd-Map cache shape as `keywords.ts` (in-memory only, no disk persistence - TMDB isn't the rate-limited API here), scoped to a hardcoded `"US"` region (see Known follow-ups). Rendered on the movie/series detail pages only (grid cards skipped - too small to be useful), grouped into Stream/Rent/Buy with provider logos and a "More info on TMDB" attribution link; renders nothing (not an empty box) when TMDB has no providers for a title. Verified via lint/typecheck/build plus a real dev-server run driven with Playwright against the live TMDB API: Interstellar (movie) and Breaking Bad (tv) both show correct real provider logos/names with no console errors (screenshotted); an obscure/invalid title (id 1) correctly renders no "Where to watch" section at all.

5. ~~Watchlist~~ — built (`src/lib/watchlist.ts`, `src/lib/use-watchlist.ts`, `src/components/watchlist-view.tsx`, `src/app/watchlist`, `src/app/api/watchlist/enrich`). Add/remove lives on the card itself (a bookmark icon, top-right of every `MovieCard`) rather than the detail page — cards appear in every context (browse, search, blend, mood search, recommendations, the watchlist page itself), so this is the one placement that lets a title be saved without leaving whatever grid it's already in; the detail page has no separate button, to avoid two controls doing the same thing. `watchlist.ts` stores only `{id, mediaType, title, posterPath}` per entry (no full TMDB objects); the new `/api/watchlist/enrich` route re-fetches details+ratings fresh on every watchlist page load (dropping any id that now 404s) rather than trusting a stale snapshot. New "Watchlist" nav link in `AppHeader`; `MovieGrid`/`MovieCard` gained an optional per-item `mediaType` so one grid can mix movies and TV (every other caller is still single-media and unaffected). Reads localStorage via `useSyncExternalStore` (`use-watchlist.ts`) - the first attempt used the classic mounted-state-plus-`useEffect` pattern, which tripped `react-hooks/set-state-in-effect` (bundled with Next 16/React 19); `useSyncExternalStore` is the built-in primitive for this exact "external store, no hydration mismatch" case and sidesteps that lint rule entirely. Hit and fixed one real bug along the way: `getWatchlist()` originally re-parsed JSON into a fresh array on every call, so `useSyncExternalStore` saw a "changed" snapshot every render and infinite-looped ("Maximum update depth exceeded") - fixed by caching the parsed array and only re-parsing when the raw localStorage string actually changes. Verified via lint/typecheck/build plus a real dev-server run driven with Playwright: empty state renders a plain message (not an error); saving two titles from the popular-movies grid shows filled bookmark icons and both appear on `/watchlist` reusing the exact same card grid; a real page reload preserves both (confirms localStorage persistence, not just React state); removing one drops it immediately, removing the last one restores the empty state; zero console errors throughout.
6. ~~"Surprise me"~~ — built (`src/components/media-explorer.tsx`). A "Surprise me" button sits right after the filter row in `MediaExplorer`, tied specifically to the genre/sort/rating filter state (`buildDiscoverParams`) rather than to whatever mode (search/mood/blend) happens to be on screen - a stray search query doesn't change what "respecting the active filters" means. On click it re-fetches page 1 of the same `discoverEndpoint` the filtered browse grid already uses (no new API route - explicitly reuses the existing discover pipeline), picks one result at random client-side, and `router.push`es straight to its detail page - the surprise is the pick, not a results view. No filters set still goes through the same call, which naturally resolves to the same broad `sort_by=popularity.desc` pool the Popular grid shows, so there's no separate "no filters" branch. Zero-result filters show a calm inline note ("No titles match the current filters.") rather than the red error text a real fetch failure gets, and don't navigate. Verified via lint/typecheck/build plus a real dev-server run driven with Playwright against the live TMDB API: no-filter click navigates to a valid random movie; with Action genre + Min IMDb 8 set, captured the actual `/api/movies/discover` request/response and confirmed both that the request carried `genres=28&min_imdb=8` and that the navigated id was a member of that response's results (not just "some navigation happened"); a forced-empty response shows the inline message with no navigation and no error; zero console errors throughout.

All six Phase 2 roadmap items are now complete.

Post-launch polish (not part of either roadmap phase):

- ~~Brand icons next to provider names and rating sources~~ — built 2026-07-10. Watch-providers (`src/components/watch-providers.tsx`) was already using TMDB's own `logo_path` per provider - confirmed, no change needed there. The ratings line (`src/components/score-badges.tsx`, "TMDB 8.5 · IMDb 8.7 · RT 73%") had no icons at all; added three small local SVGs (`public/tmdb-icon.svg`, `public/imdb-logo.svg`, `public/rt-logo.svg` - static local files, not fetched at runtime, since brand marks don't change) rendered inline before each score at `h-3` (IMDb's real yellow/black badge proportions, a stylized red/green tomato mark) - the text label for each ("TMDB"/"IMDb"/"RT") was dropped in favor of the icon alone conveying brand identity, with `alt` text preserving it for accessibility. First pass at `tmdb-icon.svg` was hand-drawn (a plain rounded-square gradient swatch, no actual logo shape) and correctly called out as not the real TMDB mark - just a colored square. Fixed by sourcing TMDB's actual short-form mark (the stacked "THE MOVIE DB" wordmark, confirmed authentic via the embedded `<title>The Movie Database</title>` and TMDB's real documented brand hex `#01B4E4`) from Simple Icons (`cdn.simpleicons.org` - a well-established repo of exactly this "static brand attribution icon" shape, one-time sourced and saved locally, not fetched at runtime), recolored with the same gradient stops as the existing `tmdb-logo.svg` footer asset for visual consistency. Verified live: screenshotted both contexts `ScoreBadges` renders in - the light-background movie detail page and a card's dark gradient-overlay hover panel - icons render crisp and legible at 12px in both (confirmed the TMDB mark specifically reads as "THE MOVIE DB" at 4x zoom, not just a blob), don't compete with the poster/title, zero console errors.
- ~~Region dropdown for "Where to Watch"~~ — built 2026-07-10 (`src/lib/watch-region.ts`, `src/lib/use-watch-region.ts`, `src/app/api/movies/[id]/watch-providers`, `src/app/api/tv/[id]/watch-providers`, `src/components/watch-providers.tsx`). US/Brazil dropdown next to the "Where to watch" heading; persisted via localStorage using the exact same `useSyncExternalStore` pattern as the watchlist (`watch-region.ts` is simpler than `watchlist.ts` though - it stores a single string primitive, which is already `Object.is`-stable across separate reads, so it doesn't need `watchlist.ts`'s parsed-array-caching workaround). `watch-providers.ts`'s cache is now keyed `${mediaType}:${id}:${region}` instead of `${mediaType}:${id}` (TMDB's own endpoint already returns every region in one response - this still fires one call per region rather than caching the whole multi-region payload once, matching ratings.ts/keywords.ts's one-value-per-key shape as asked, and costs nothing that matters since TMDB isn't rate-limited here). `WatchProviders` is now a client component: the detail pages still server-fetch the default region (US) for a flash-free first paint in the common case, and the component compares that against the real persisted region on mount, re-fetching via two new API sub-routes only when they differ. Design call beyond the literal ask: the heading+dropdown now render unconditionally (previously the whole section, heading included, hid itself when a title had zero providers) - with a user-selectable region, hiding the *selector itself* whenever the current region happens to be empty would trap a user who might see data in the other region; only the Stream/Rent/Buy rows stay conditional on `groups.length > 0`. Verified live against the real TMDB API: Interstellar shows genuinely different providers per region (US: Hulu/Paramount+; BR: HBO Max/Claro tv+/Apple TV Store), switching updates in place with no page reload and no stale entries from the other region; a real page reload after switching to Brazil persisted the selection and auto-re-fetched BR data without touching the dropdown again; an invalid `region` query param on the API route returns a clean 400; a title with valid details but zero providers in any region (a very obscure 1898 short film, found by querying TMDB discover for `vote_count.asc` before 1930) shows the dropdown with no rows and no error, exactly as designed.
- ~~Homepage hero redesign~~ — built 2026-07-10 (`src/components/hero-search.tsx`, `src/components/interpretation-chips.tsx`, `src/components/header-search.tsx`, `src/components/media-explorer.tsx`, `src/components/app-header.tsx`). Layout/hierarchy change only - every fetch call, cache, and API route from the six Phase 2 features stayed untouched; only the render layer and a few pure-UI state values changed. Mood search promoted to a centered hero (`hero-search.tsx`): "What are you in the mood for?" (the browse page's one Bebas Neue moment) + a single gold button that follows whichever action occupies the hero slot (Find by default, Blend once swapped into blend view via a new `heroView` state - they never coexist, so "one gold button on screen" holds automatically) + a quiet row of three text+icon entry points (Blend two titles / Surprise me / Browse with filters). "Browse with filters" is a toggle (`filtersRevealed` state) that hides the existing search+`FilterPanel` row without clearing its state - a `hiddenFilterSignal` count (reusing `activeFilterCount`, extended to also count a non-empty quick-search query) badges the collapsed entry point so results never look mysteriously filtered with no visible cause. `InterpretationChips` upgrades the old plain-text "Interpreted as: …" line into individually-legible display-only pills (genres/keywords/year-range), still with a "Clear" link. Every results header (mood/blend/search/filtered-browse, not just mood) now renders as quiet normal-weight `{context} · N results` instead of Bebas Neue - discover mode's count needed a new `discoverTotalResults` state (captured from the discover API response's already-present `total_results` field) since `items.length` there is only "currently loaded," not the true match count, unlike mood/blend/search which aren't paginated in this UI. New `HeaderSearch` in the nav (`header-search.tsx`) is a fully self-contained quick-lookup for power users - same debounce/abort/request-id shape as `TitlePicker`, but selecting a result (click, or Enter picks the top match) navigates straight to the title's detail page rather than selecting into a controlled value; deliberately has no URL-param/grid-search fallback path (dropped during planning to avoid wiring `MediaExplorer` to read initial state from the URL for a secondary path that wasn't the one that mattered). Hit one real bug while verifying at mobile width: the header (logo wordmark + new search icon + Watchlist link + Movies/Series toggle) didn't fit at ~390px and the wordmark visibly overlapped the search icon - fixed by hiding the "KINDRED" text below the `sm` breakpoint (keeping just the logo mark) and tightening header gaps responsively, a pre-existing marginal fit the new icon tipped over the edge, not a hero-specific bug. Verified live across all five states: hero default, blend swap (+ "← Mood search" back link restoring Find), filters revealed → genre set → collapsed (confirmed the grid stayed filtered and the entry point showed a "1" badge, heading read "Filtered · 6298 results" - a real TMDB total, not just the 20 loaded), a real mood search end-to-end ("Slow melancholic sci-fi · 20 results" with genre/drama chips rendering, matching the brief's own example verbatim), the header search navigating to Interstellar's real detail page from a live dropdown match, and mobile width (390px, post-fix, no overlap). `/series` smoke-tested too (shared `MediaExplorer`) - "Popular series" heading renders correctly, zero console errors.
- ~~Pre-launch prep~~ — 2026-07-10. Real `README.md` (what Kindred is, tech stack, full `.env.local` setup) replacing the `create-next-app` boilerplate; a deploy-readiness audit came back clean across the board (production build passes from a clean `.next` and a booted `next start`, no hardcoded localhost/dev-only assumptions, all secrets read via `process.env` only with nothing ever committed - checked against the full git history, not just current source - `.gitignore` covers `.env*`); new CLAUDE.md Deployment notes section on the Vercel-serverless in-memory-cache tradeoff. One finding worth recording: only `TMDB_API_TOKEN` is a hard requirement - `OMDB_API_KEY`/`MDBLIST_API_KEY`/`ANTHROPIC_API_KEY` all degrade gracefully when unset (already true in the code, not a new behavior), documented precisely in the README rather than calling all four uniformly "required".
- ~~Rate limiting on the Anthropic-backed routes~~ — built 2026-07-10 (`src/lib/rate-limit.ts`, wired into `movies/mood-search`, `tv/mood-search`, `explain-match`). Simple in-memory fixed-window counter, 10 requests/minute/IP per route (three independent buckets, not a shared pool), explicitly no Redis/KV - defense-in-depth alongside the real backstop (the Anthropic spend cap), not a substitute for it; documented as best-effort in CLAUDE.md's Deployment notes since Vercel serverless doesn't guarantee the same function instance (and thus the same in-memory counter) handles every request from one IP. `getClientIp` reads `x-forwarded-for` (Vercel's own convention) with an `x-real-ip`/`"unknown"` fallback for local dev. The mood-search 429 gets a distinct, deliberately calm UI treatment rather than reusing the red `moodError` path: `moodQuery` rolls back to empty (so `mode` reverts away from "mood" instead of leaving the grid pinned on an unresolvable loading skeleton - `moodResults` would otherwise stay `null` forever) and a new `moodRateLimitMessage` renders as a quiet note in the same slot as the existing "Mood search — coming soon" text. `explain-match`'s 429 needed no client changes - its existing "Try again" + tooltip mechanism already surfaces whatever message the server sends. Verified live: hammering `/api/movies/mood-search` with a fixed simulated IP (`X-Forwarded-For`) allowed exactly 10 requests then returned clean 429s with a `Retry-After` header; a different simulated IP was unaffected (confirms per-IP isolation, not a global limiter); `explain-match` independently rate-limited the same way; drove the actual UI to trigger a 429 and confirmed the calm message shows, no red error text, no stuck skeleton, and the Popular grid renders normally underneath; a normal single search from a fresh IP still completes end-to-end with zero console errors.
- ~~Filters compose with mood search + streaming-platform filter~~ — built 2026-07-10, prompted by the first round of real (non-Claude) user feedback: clearing an active mood search just to add a rating or genre filter felt wrong once actual people tried it, since mood search was previously mutually exclusive with genre/sort/rating filters (see the now-superseded Known follow-up below). Genre/sort/rating/streaming filters now merge into an active mood search instead of clearing it; plain quick-search text is unchanged and still clears mood (that pairing was never the complaint - text search and mood search answer the same question two different ways, so staying mutually exclusive is still correct). Server side: `movies/mood-search` and `tv/mood-search` now branch on the POST body - a `query` string still runs the full LLM interpret+resolve (rate-limited, unchanged); a `cachedInterpretation` (the already-resolved genre/keyword ids+names, a new `ResolvedMoodParams` shape in `src/lib/mood-search.ts`) skips the LLM entirely and re-runs `discoverWithMoodFallback` against it - not rate-limited, since it never touches Anthropic. Both paths accept the same optional overrides (`genreIds`, `sortBy`, `minImdb`, `minRt`, `watchProviderIds`, `watchRegion`), merged via a new `applyMoodFilterOverrides`: an explicit genre override wins outright over the LLM's own genre extraction, and - since it's now the user's own checkbox pick, not the AI's inferred hard filter - switches genre matching from mood's AND semantics to the browse page's normal OR semantics. `minImdb`/`minRt` apply as a post-fetch filter via `ratings.ts`'s existing `passesRatingFilters` (mood search had no rating filtering at all before this - only plain discover did). The "Interpreted as…" chips intentionally keep showing the LLM's own original extraction even when genre is overridden, not the applied/overridden set - showing the user's own filter pick back to them inside the "AI interpreted this" chips would be redundant with the genre checkboxes already visible in the filter panel, and would misattribute a user choice to the model. New streaming-platform filter: `STREAMING_PROVIDERS` in `src/lib/watch-providers.ts`, a curated six-per-region checklist (not TMDB's full hundreds-long list) with ids verified directly against the live `/watch/providers/movie` API rather than guessed - the same nominal service can have a different id per region (Prime Video is 9 in the US, 119 in Brazil). Reuses the existing region selection (`use-watch-region.ts`, the same localStorage state "Where to watch" already drives) as `watch_region`; new `StreamingFilter` component mirrors `GenreFilter`'s pill-button pattern. `discoverMovies`/`discoverTV` gained `watchProviderIds`/`watchRegion` params (`with_watch_providers` OR'd, `with_watch_monetization_types=flatrate` scoping to subscription availability, not rent/buy), threaded through both the mood-search routes and the plain `movies/discover`/`tv/discover` routes, so the filter composes with mood, plain browse, and (via the existing `buildDiscoverParams` reuse) Surprise Me too. Client-side, `toggleGenre`/`updateSortBy`/`updateMinImdb`/`updateMinRt`/the new `toggleWatchProvider` no longer clear mood (only blend still does - blend's fixed candidate pool doesn't compose with filters, unchanged). A new `runMoodDiscovery`/`moodFetchKeyRef` mechanism avoids re-running the LLM on every filter tweak: `moodFetchKeyRef` snapshots the filter state a mood fetch is about to reflect, and a `useEffect` watching genre/sort/rating/streaming/region compares against it to fire a `cachedInterpretation` refetch only on a genuine filter change, not merely because `moodResolvedParams` itself just settled from the same fetch. A real bug turned up during verification, not just a design risk: the existing plain-discover `useEffect` fires on any filter change whenever `filtersActive` is true, with no awareness of `mode` - previously harmless because a filter change always cleared mood first (so `mode` really had fallen to `"discover"` by the time it ran), but once filter changes stopped clearing mood, it kept firing a wasted parallel `/api/movies/discover` request on every mood+filter tweak (nothing rendered it - `mode` stays `"mood"` - but it burned a real TMDB+OMDb round trip for nothing). Fixed by gating that effect on `moodQuery === ""` too, mirroring the same precedence `mode`'s own ternary already encodes. Verified live against the real Anthropic+TMDB+OMDb APIs: "slow melancholic sci-fi" (→ Science Fiction+Drama) plus a Comedy genre override correctly flips results to Comedy titles (Parasite, Toy Story 3, Boogie Nights) while the chips keep showing the original Science Fiction/Drama interpretation, not Comedy - confirmed user-filter-beats-LLM precedence directly, not just by code inspection; adding a Netflix streaming filter on top of that same mood+genre combination correctly narrowed further; plain browse (no mood) plus a Disney+ filter correctly surfaced Disney's own catalog (Toy Story, Zootopia, Avengers, Avatar); the collapsed "Browse with filters" badge correctly counted the streaming filter alongside genre (read "2"); captured the actual request payloads through a driven browser session and confirmed a filter change sends `cachedInterpretation` with no LLM round trip while the initial submit still sends `query`; re-ran lint/typecheck/build clean after the discover-effect fix. See Known follow-ups for what's still untuned.
- Fixed a real-user-reported bug on 2026-07-10, filed right after the entry above shipped: "the initial /movies browse grid only loads a partial set (~18 tiles) and stops." Initial suspicion (from the report itself) was that the discover-effect's new `moodQuery` gate was also swallowing plain pagination - directly disproven by driving Load More through several pages with mood untouched (requests and item counts both came back correct) on both `next dev` and a local production build. The real cause, found from a user-provided screenshot: `gridItemVisibilityClass` (`src/lib/grid-visibility.ts`, pre-existing, introduced back in the watchlist commit `1f8a074` - confirmed unrelated to this deploy by reproducing the identical split in an isolated `git worktree` checkout of the commit before it) deliberately hides a grid's trailing partial row per breakpoint to avoid a ragged edge. That's fine when the hidden items stay reachable (Discover mode's "Load more" will eventually reveal them) but was being applied unconditionally to every grid, including the ones with no pagination at all (Popular, mood/blend/search results, watchlist, both detail-page "More like this" grids) - so on any viewport wide enough to trigger the trim, 2-4 real, already-fetched results were just permanently unreachable with no error and no visible cause. `.count()`-based Playwright checks completely missed this during initial investigation since it counts DOM nodes regardless of `display:none` - had to switch to checking `offsetParent !== null` to actually see the discrepancy. Fixed by making the trim opt-in: `MovieGrid` gained a `trimTrailingRow` prop (default `false`, meaning "show everything, accept a ragged last row"), and `media-explorer.tsx` is the only caller that passes it, wired to the existing `canLoadMore` (`trimTrailingRow={canLoadMore}`) - true only in Discover mode while another page can still be fetched, false the moment the last page loads (closing the same "unreachable" gap for Discover's own final page, which the initial fix wouldn't have caught otherwise) and false in every other mode. `watchlist-view.tsx` and both detail-page recommendation grids don't pass the prop at all, so they default to showing everything. Verified live: unfiltered Popular grid now shows all 20 (was 18) across four different viewport widths; mood search results and a detail-page recommendations grid both show every fetched item; a Discover query clicked through to genuine exhaustion (real "Load more" clicks until the button disappeared, not a premature stop - the first verification attempt falsely triggered on the button's transient "Loading…" text) ended with all 120 accumulated items visible, zero hidden. Mid-pagination (more pages still available) still trims to *approximately* a clean row - see the note in Known follow-ups on the leftover cosmetic imprecision there, unrelated to reachability.
- ~~Popular grids get Load More + the clean-edge trim~~ - built 2026-07-10, prompted by real usage: 20 titles was too few, and the ragged partial row the previous fix now deliberately shows at the end of Popular (accept a ragged edge over hidden content) looked unfinished. TMDB's `/movie|tv/popular` were already paginated under the hood (`getPopularMovies`/`getPopularTV` already took a `page` argument, and `/api/movies|tv/popular` already accepted a `page` query param - both untouched, no server-side work needed) - the whole feature is a `media-explorer.tsx` client change plus threading TMDB's `total_pages` down from the server component. `MediaExplorerConfig` gained a `popularEndpoint`; `MediaExplorer` gained an `initialTotalPages` prop (`movies/page.tsx`, `series/page.tsx`, and `movies-view.tsx` now pass `popular.total_pages` through, mirroring how `initialItems` already carried `popular.results`). New `popularItems`/`popularPage`/`popularTotalPages`/`popularLoadingMore` state plus a `loadMorePopular` mirroring `loadMoreDiscover`'s accumulate-on-success shape exactly (`setPopularItems((prev) => [...prev, ...data.results])`); `canLoadMore` and the Load More button/handler are now unified across Discover and Popular (`loadingMore`/`handleLoadMore` dispatch on `mode`) rather than being Discover-specific, so `trimTrailingRow={canLoadMore}` - already wired from the previous fix - covers Popular for free with no separate prop needed. One deliberate deviation from Discover's pattern: a failed `loadMorePopular` does NOT feed into the shared `error` variable that replaces the whole grid with red text - Popular's page 1 is always server-rendered and reliably present, so wiping already-visible, already-successful content over a later page's fetch failure would undermine the exact "don't lose reachable content" principle this whole feature is about; it renders a quiet inline note near the button instead (same category as `surpriseMessage`), leaving Discover's existing (pre-existing, out of scope here) grid-wiping behavior on its own failures untouched. Verified live across four viewport widths (1440/1280 = 8-col, 1000 = 6-col, 700 = 4-col): initial load already ends on a complete row at every width tested; repeated Load More clicks correctly accumulate (20→40→60, checked at each step) and re-settle onto a clean row each time; a first verification pass falsely read "Load More disappeared after one click" at 1440px - actually just checking mid-fetch while the button showed its own disabled "Loading…" text, same class of test-timing mistake hit twice already in this session, fixed by waiting out that state before asserting; a clean full-width screenshot confirms 7 full rows (56 of 60 fetched) with the Load More button sitting flush beneath, no gap. Watchlist and mood/blend results deliberately left untouched (still show everything, no pagination) per the explicit scope of this pass.
- ~~Mood search reranks by tone instead of trusting popularity.desc alone~~ - built 2026-07-13, closing the "popularity.desc has no notion of tone" deferred item below (Parasite/The Wolf of Wall Street surfacing in "cozy feel-good" results; the mistagged-obscure variant of the same problem). Went through three iterations before landing, each one surfacing a real problem the previous design didn't anticipate - worth recording all three since the final shape only makes sense in light of what it replaced:
  1. First cut: keywords stopped being a hard filter-or-nothing switch and became a per-candidate ranking signal. Pool fetched 2-3 pages deep (~40-60 candidates) by genre AND (unchanged) + `vote_count.gte=200` (new `voteCountGte` override on `discoverMovies`/`discoverTV`, matching the existing `TOP_RATED_MIN_VOTE_COUNT` value - verified live to keep pools comfortably large: 291 candidates for Science Fiction+Drama, 1,590 for Comedy+Drama), each candidate's keywords fetched via the shared `keywords.ts` cache, scored by reusing `match-explanation.ts`'s existing `computeMatchSignals`/`blendSignalScore`/`explainMoodMatch` as-is (no new weights - `GENRE_POINTS_ONE=1`, `KEYWORD_POINTS_ONE=2`), stable-sorted (pool arrives pre-sorted by popularity/vote_average, so ties keep that order as an implicit tiebreak, same pattern `vibe-blend.ts` already used), top 20 taken. Verified live against real TMDB data first: this alone did NOT fix the regression - Parasite and The Wolf of Wall Street still appeared, because with genre AND'd every pool candidate shares the same baseline score, and TMDB's keyword tagging for abstract tone words is sparse enough (measured directly: "feel-good"/"cozy" etc. cover as few as 5-26 movies in the ENTIRE catalog) that only ~7 of 60 popularity-sorted candidates carried any of the 5 extracted keywords - the other 13 "top 20" slots still got padded from the same zero-match, high-popularity tier Parasite/Wolf of Wall Street belong to, undoing the fix by padding it right back in.
  2. Second cut: pool became a union of two independently-fetched sets - a "precision" set (genre AND + keywords OR as a hard filter, fetched unconditionally and kept however few results it has, no give-up threshold) and a "bulk" set (genre-only, for volume, keeping the old genre-narrow-to-primary fallback via `MIN_MOOD_RESULTS`). This guarantees every genuine keyword match is in the pool regardless of its raw popularity rank, not just whatever happens to leak into a popularity-sorted top-60. Verified live: did NOT change the outcome either - queried TMDB directly and found the TRUE keyword-tagged universe for "cozy feel-good" (Comedy+Drama AND cozy/feel-good, vote_count≥200) is only 3 titles; with the model's full 5-keyword extraction, just 1. This wasn't a pool-depth problem at all - a hard data-sparsity ceiling no amount of pool-widening can fix, since the precision set already finds every genuine match, however few. With only 1-3 real matches, the remaining 17-19 slots still had to come from the same zero-match popularity tier. Considered (and explicitly rejected, per user direction) capping results below 20 rather than padding - checked against the *other* regression case ("slow melancholic sci-fi") and found it has the identical sparse-keyword-universe shape (only 6 genuine matches at vote_count≥200) yet is explicitly required to stay at its current ~20-result quality, meaning a flat "don't pad" rule would have broken a case that was supposed to be preserved, not fixed - the two regressions need opposite treatment from a matching-count-only signal, which no threshold on match count alone can distinguish.
  3. Final shape: added a per-candidate genre-clash penalty instead, restoring full padding to 20 (removing the never-actually-shipped "cap below 20" idea) while making the padding itself tone-aware. `interpretMoodQuery`'s schema gained an `avoidGenres` field (`MAX_AVOID_GENRES_FROM_QUERY=4`) - genres whose presence on a candidate would work against the mood's tone, resolved fresh per query rather than a maintained static list specifically so it stays mood-*relative*: a cozy/feel-good mood avoids Thriller/Crime/Horror/War, while a tense/dark mood avoids Comedy/Family/Animation instead of avoiding itself (the schema description explicitly models both directions - without that, the model defaults to treating it as a fixed "sounds dark" list, which is exactly what a "tense slow-burn thriller" query would wrongly get penalized by). Threaded through `ResolvedMoodFilters`/`ResolvedMoodParams` (`avoidGenreIds: number[]`, defaulting to `[]` in `fromResolvedMoodParams` so a cached interpretation saved before this shipped never crashes, just gets zero penalty). Penalty: `AVOID_GENRE_PENALTY = 1` point per clashing genre a candidate actually carries, subtracted in `discoverAndRankMoodPool`'s scoring loop (kept local to mood-search.ts, not added to match-explanation.ts's shared primitives - blend has no notion of this). Weight chosen to preserve the evidence hierarchy: strictly smaller than `KEYWORD_POINTS_ONE` (2), so a real keyword match plus exactly one clash genre still outranks an untagged clean candidate (2 genre baseline + 2 keyword − 1 clash = 3, vs. 2 baseline + 0 = 2) - verified by hand, not just assumed. Two or more simultaneous clash genres CAN invert that against a single-keyword match; left uncapped deliberately (not treated as a bug) since a title clashing on multiple genres at once is a genuinely worse fit even with an incidental keyword tag, and the hierarchy guarantee was only ever scoped to the one-clash case. Verified live across both regressions plus a third, deliberately adversarial case chosen specifically because a *static* avoid-list would fail it and a mood-relative one passes: "tense slow-burn thriller" resolved `avoidGenreIds` to Comedy/Family/Animation only (never Thriller/Crime/Horror/Mystery, confirmed via the response's `resolvedParams.avoidGenreIds`), returning a genuinely tense 20-result list (Se7en, Rear Window, The Usual Suspects, Rope, Chinatown, The Third Man). "Cozy feel-good comedy for a rainy day" re-verified with the final mechanism: Parasite and The Wolf of Wall Street both gone, grid still a full 20 (Forrest Gump, The Intouchables, Green Book, Life Is Beautiful, La La Land, Mean Girls among them), zero console errors in a driven browser session. "Slow melancholic sci-fi" re-verified unaffected/improved: still 20 results, all genuinely contemplative (Eternal Sunshine, Blade Runner 2049, Interstellar, Stalker, Metropolis, Her, La Jetée). Re-ran the `avoidGenres` extraction across 4 more diverse moods for consistency (matching the date-anchor fix's own "verify across several moods, not one" lesson) - two genre/plot-based queries ("epic space opera with a found family crew", "funny heist movie") both correctly resolved `avoidGenreIds` to empty, confirming the field doesn't fire spuriously on moods without real tonal polarity; TV mood search checked too (`avoidGenreIds` resolved to Crime/War & Politics/Mystery for a TV "cozy feel-good comedy" query - TV's own genre taxonomy, correctly distinct from movies'); the `cachedInterpretation` filter-composition flow (a sort/genre tweak on an already-active mood, no fresh LLM call) confirmed to carry the penalty through correctly - Parasite/Wolf of Wall Street stayed excluded after a cached-path re-rank.
  - Cost/latency, measured directly rather than estimated: cold-cache fresh queries observed at 3.5-13.8s end-to-end (Anthropic call + pool fetch + up to ~60 parallel keyword lookups + OMDb rating enrichment on the final 20), well within the LLM-backed search's existing latency envelope. TMDB call volume roughly triples versus the pre-rerank implementation (precision + bulk pool pages, plus one keyword lookup per pool candidate instead of per final-20-result) - not separately capped, matching this app's existing "TMDB isn't the rate-limited API here" tolerance already established by `vibe-blend.ts`'s per-candidate keyword calls. OMDb cost is unchanged: rating enrichment still runs only on the final ranked 20, never the full pool.
  - Chip-display side effect, called out explicitly at proposal time: the "Interpreted as" keyword chips now always show the full resolved keyword set instead of only whichever subset survived the old hard-filter fallback, since keywords are now always a ranking signal regardless of whether they ever constrained the pool. `avoidGenreIds` itself is deliberately NOT surfaced in any chip or UI text (no "why this doesn't match" display exists) - purely an internal ranking input.
- Fixed a genre-extraction instability found via real usage on 2026-07-13, right after the reranking feature above shipped: "cozy feel-good comedy" (the same mood, minus " for a rainy day") sometimes extracted only Drama - Comedy silently missing despite being the literal word in the query - producing wrong-toned results (Damage, Titanic, Logan, Farewell My Concubine). Investigated with direct, repeated calls against the live Anthropic API (bypassing the app, to isolate extraction from everything downstream) rather than guessing: 44 initial trials of the exact reported phrase never dropped Comedy even once (Drama dropped ~7% of the time instead - the opposite pattern), so the bug didn't reproduce until testing nearby phrasings - reordering to "feel-good cozy comedy" (tone words before "comedy" instead of after) reproduced a genuine 25% Comedy-drop rate (2/8), confirming real phrasing-sensitive instability in the 2-genre extraction, not a one-off fluke. `avoidGenreIds` stayed correctly populated in every single trial regardless of whether genre extraction was strong or weak, including both reproduced Comedy-drop cases - contrary to the initial report's suspicion, the two extractions are NOT correlated in this codebase's testing, so `avoidGenres` did not need the reported symptom's fix (though it already had - and keeps - the same both-polarity prompt treatment from its own design pass). Root-caused to the `genres` field's schema description having no guidance at all about literal genre words carrying extra evidentiary weight - the model was inferring genre entirely from tone words with no anchor to the query's own literal text. First fix attempt (a blunt "literal mention is strong evidence, include it" instruction) tested clean for Comedy (40/40 across 4 phrasings) but introduced a new regression, caught before shipping by testing the *other* genre too: Drama collapsed to near-zero (0-2/10) across the same phrasings, because the instruction read as "the literal match is basically the whole answer" and suppressed the model's willingness to also infer a second, non-literal genre on top. Refined to explicitly frame a literal mention as "a floor, not a ceiling" - secures the literally-named genre unconditionally, but explicitly instructs the model to keep separately considering whether other genres are ALSO implied by the rest of the query - retested clean on both axes: 40/40 Comedy AND 40/40 Drama across the same 4 phrasings, `avoidGenres` still never empty. Regression-checked against the full established suite before applying to the codebase (sci-fi, thriller, and both negative-control moods) - all stable, and worth noting explicitly: "slow melancholic sci-fi" now resolves `avoidGenreIds` to Action/Comedy/Horror where it was previously empty (the refined prompt's explicit both-polarity framing makes the model more willing to name clash genres for tonally-charged moods generally, not just this one) - verified live through the full pipeline that this doesn't regress the result set, since Interstellar and Blade Runner 2049 (both carrying a secondary Action tag on TMDB) still rank at positions 1/3, exactly matching the scoring hierarchy's guarantee that one clash genre can't outweigh a real keyword match. Verified live end-to-end after applying: the exact originally-broken query now consistently resolves Comedy+Drama across 5 repeated real requests (each a fresh Anthropic call, no caching involved), Damage/Titanic/Logan absent, grid a full 20; "cozy feel-good comedy for a rainy day", "slow melancholic sci-fi", and "tense slow-burn thriller" all re-verified unaffected through the real API route (not just the isolated extraction script).
- ~~Movies/Actors tab row removed from the nav~~ - built 2026-07-13. The "Movies" tab was redundant with the header's own Movies/Series toggle (same word, two highlighted controls competing for attention); actor browsing is now implicit-only, reachable via cast lists on detail pages (unchanged - they already linked to actor pages) rather than a dedicated top-level entry point. Checked what the tab actually offered before removing it, not just what it looked like: the "Actors" tab was a full debounced name-search (`/api/people/search`) with a "Trending actors" default grid - real, reachable functionality, not just a browse affordance, so it needed a real replacement rather than just deleting it. Folded into `HeaderSearch` (the existing title quick-lookup in the nav): a query now fires title search and `/api/people/search` in parallel (`Promise.allSettled`, not `Promise.all` - a people-search failure shouldn't blank out title results that already succeeded, or vice versa), with matches grouped under a "People" label using the same section-header style `GenreFilter` already established. Enter still prioritizes the top title match (titles remain the primary case this component exists for) and only falls back to the top person when a query matches a name but no title at all - a pure actor-name lookup. Clicking a person result navigates to `/actors/{id}`, the existing (unchanged) actor detail route. `movies-view.tsx` lost its tab state entirely along with the `ActorSearch` import, dropping its own `"use client"` directive in the process - with no hooks of its own left, it's now a plain Server building block again, same as how `series/page.tsx` renders `MediaExplorer` directly, no client-only wrapper needed. `actor-search.tsx`, `actor-grid.tsx`, and `actor-card.tsx` were fully orphaned by the tab's removal (grepped first to confirm no other caller - the actor detail page uses a separate, unrelated `ActorFilmography` component) and deleted outright rather than left as dead code; `movies/page.tsx` stopped fetching `getPopularPeople()` to seed the now-gone tab's default grid. `/api/people/popular` itself was already unreachable before this change (nothing ever fetched it client-side; `getPopularPeople()` was always called directly server-side) and is unrelated to this pass, so left alone. Caught a real, pre-existing mobile bug while verifying "mobile width still clean" per the ask: `HeaderSearch`'s results dropdown (`w-64`, anchored `right-0` to the input) has always overflowed off the left edge of narrow viewports, since the collapsed mobile header sits the input close to the screen's left edge with nowhere for a right-anchored fixed-width dropdown to expand into - pre-existing (unrelated to this pass's own width/positioning code, which was never touched), but now visibly worse with real content added, and squarely inside the file already being edited to satisfy this task's own verification checklist, so fixed alongside it: `left-0` on mobile, `sm:left-auto sm:right-0` reverting to the original desktop-safe anchor at the `sm` breakpoint and up. Verified live: cast list on a movie detail page still navigates to a real, fully-populated actor page (confirmed via an explicit `waitForURL` before reading the destination - an early attempt without it read the H1 before the client-side transition landed and produced a false negative); header search for an actor name surfaces a title match plus a correctly-scoped "People" section (also required a real fix during testing - the first click-test used a `hasText` locator that ambiguously matched a title result whose text happened to also contain the actor's name as a substring); clicking the person result lands on `/actors/{id}`; both desktop and mobile widths screenshotted post-fix with the dropdown fully on-screen at both; zero 404s across the whole flow; build/lint/types clean.
- ~~Genre filter overrides intersect with an active mood instead of replacing it~~ - built 2026-07-13, prompted by real usage: checking a genre during an active mood search REPLACED the mood's own genres outright (hard AND -> OR checkbox semantics, from the original filters-compose design), and twice produced results that felt wrong - "slow melancholic sci-fi" (Science Fiction+Drama) plus checking Adventure returned Wizard of Oz and Meet the Robinsons, the sci-fi constraint just gone. `applyMoodFilterOverrides` (`src/lib/mood-search.ts`) now returns an ORDERED list of genre attempts (`MoodGenreAttempt[]`) instead of a single genre set: with an override active, attempt 0 is mood genres ∪ user genre, all AND'd (deduped, so picking a genre the mood already extracted is a no-op); attempt 1, the fallback, is the user's pick alone, OR mode - today's old default, now demoted to "only if the intersection is too thin." `discoverAndRankMoodPool` generalizes its existing bulk-pool cascade (previously hardcoded to the no-override "narrow 2 mood genres to 1" case) into a genuine walk over `genreAttempts`, trying each until one clears `MIN_MOOD_RESULTS` on page 1 - the SAME already-tuned threshold from the earlier reranking work, reused rather than adding a second "how thin is too thin" concept to the file (user's own call: strict-zero would let a 1-2-result intersection through, which reads about as broken as empty). `discover`'s callback signature gained `genreMatchMode` as a per-call argument instead of a value baked into the closure, since a single pool build can now need both AND and OR fetches. The precision (keyword) pool always uses attempt 0 regardless of what the bulk pool falls back to, unchanged from its existing "take whatever genuine matches exist, no relaxation" design. Verified live against the case that motivated this: sci-fi mood + Adventure override correctly surfaces Interstellar (this fix's central payoff, explicitly the assertion that mattered most) with Wizard of Oz/Meet the Robinsons gone; the user's OTHER expected title, Project Hail Mary, does NOT appear - checked directly against TMDB and confirmed its real genre tags are Science Fiction+Adventure only, no Drama, so it genuinely fails the strict AND even though it colloquially fits the mood - a real data-tagging gap, not a bug, reported rather than silently accepted. Verified the fallback itself with a genuinely contradictory pick (mood "cozy feel-good comedy" + checking Horror): correctly falls back to Horror-only and returns 20 real horror-comedy-adjacent titles (Fido, The House, One Cut of the Dead) rather than an empty grid; looked at the live UI transition specifically as asked (not just the API response) - the results header and mood chips keep showing the ORIGINAL "cozy feel-good" text/tone-words while the grid visibly swaps to horror posters, a real but minor mismatch between the surrounding text and the content; judged acceptable for this task's scope (the gold-highlighted Horror pill and "Filters 1" badge already signal an active filter, and horror poster art is unambiguous on its own) rather than fixed, since fixing it would mean plumbing a new "was this a fallback" signal through to the client for a messaging-only change outside what was asked - logged as a conscious call, not a default. A multi-genre override (mood sci-fi + Adventure + Fantasy, a 4-genre AND) surfaced only 10 results instead of a full 20 - investigated rather than assumed a bug, and confirmed directly against TMDB that the real catalog has *exactly* 10 titles satisfying all four genres simultaneously (Stand by Me Doraemon 2, The Secret of NIMH, Green Lantern: Emerald Knights among them) - genuinely correct behavior at the boundary of the already-established "10 is comfortable" threshold, not a new bug, since a narrower-but-real 10-title intersection is exactly what "intersect, don't needlessly broaden" should produce. TV parity checked with "tense slow-burn thriller" + a Mystery override (`Dark`, `The Twilight Zone`, `FROM` - genuinely fitting the combination); the `cachedInterpretation` filter-composition path (no fresh LLM call) used for every override test above, confirming the new precedence composes correctly with that flow, not just fresh queries.

## Known follow-ups

- `gridItemVisibilityClass`'s trim is imprecise mid-pagination (Discover and now Popular both use it): for a 20-item page at the 8-column breakpoint the math calls for showing 16 (a clean two full rows) and hiding the trailing 4, but 18 actually render - some of the "should be hidden" items aren't getting hidden, most likely a Tailwind cascade issue between the dynamically-built `sm:block`/`md:block`/`xl:hidden`-style utility strings rather than the trim math itself (the math was checked by hand and is correct). Widened from an earlier note that only pinned this at the md/xl boundary - a 2026-07-10 pass testing Popular's Load More across four viewport widths (base/sm/md/xl) saw the same class of off-by-a-few-items imprecision at every breakpoint transition, not just md→xl, so it looks like a general property of the dynamic-class-string approach rather than a one-breakpoint edge case. Confirmed pre-existing (reproduced identically on the commit before the filters-compose deploy) and, per the reachability fix, no longer a *reachability* problem anywhere it's used - the extra items are bonus-visible, not hidden-and-lost, and everything converges to fully visible once pagination reaches a cleanly-divisible total or is exhausted (verified: Discover to 120/120, Popular to 60/60 at two of the four tested widths). Purely cosmetic (rows look a little less tidy than intended mid-pagination) and out of scope for both fixes so far; worth a proper look - most likely rewriting the visibility check to not rely on dynamically-interpolated Tailwind class names at all - if the trim's row-cleanliness ever matters enough to chase.
- OMDb's free-tier quota reset (2026-07-09) and ratings now populate correctly end-to-end - confirmed via a fresh blend request (Inception + Arrival, real `imdbRating`/`rottenTomatoesScore` values returned) and a detail-page visit. `getRatings`'s optimistic-marking dedup (see `src/lib/ratings.ts`) is unchanged by any of today's fixes; three concurrent requests for the same uncached title resolved consistently with no errors or races, but the exact "one OMDb call, not three" claim is still not directly instrumented (an attempt to log real call counts hit a shell-redirection issue with the backgrounded dev server, not a code problem) - low priority now that the broader "does this even work against the real API" question is answered, but worth a proper instrumented check if OMDb quota exhaustion becomes a recurring problem again.
- Mood search verified against the real Anthropic API on 2026-07-10 (see build plan item 1) - interpretation quality is good and the genre-AND bug it surfaced is fixed. Mutual-exclusion UI verified the same day: submitting mood correctly clears an active quick-search value and genre selection (confirmed via input value + `aria-pressed`, not just visually); typing in quick-search or toggling a genre while mood is active correctly resets `moodQuery`/results (heading and grid switch away from mood, re-verified this wasn't just a visual mask). That pass surfaced one real gap: `clearMood()` reset `moodQuery`/results/interpretation but not the mood text *input* itself, so the box kept showing stale text after another mode took over even though mood was no longer active - fixed by adding `setMoodInput("")` to `clearMood()` (`src/components/media-explorer.tsx`), verified against all three paths that call it (typing elsewhere, toggling a genre, the explicit "Clear" link). ~~The genre/sort/rating-filter half of this mutual exclusion~~ - superseded 2026-07-10 by the "Filters compose with mood search + streaming-platform filter" entry above: those filters now merge into an active mood search instead of clearing it, prompted by real user feedback that clearing a mood search just to add a filter felt wrong. The quick-search-text half described in this note is unchanged and still correct - plain search still clears mood.
- ~~Mood search's `sort_by=popularity.desc` default ranks purely by genre/keyword membership plus raw popularity, with no notion of tone~~ - resolved 2026-07-13, see the "Mood search reranks by tone" entry above. Keyword-tag overlap is now a ranking signal (not filter-or-nothing) and a mood-relative genre-clash penalty (`avoidGenres`) demotes tonally-opposite candidates like the Parasite/Wolf-of-Wall-Street example this note originally described - both confirmed gone from that exact query's results, grid still a full 20.
- Vibe blending's score weights (genre 1/2, keyword 2/4, recommendation 2/6 - see `src/lib/vibe-blend.ts` and `src/lib/match-explanation.ts`, which now share these) are a reasoned starting point, not tuned against real usage. Revisit once there's a sense of whether results skew too genre-heavy, too obscure, or too `/recommendations`-dominated. The OMDb burst this used to cause on every blend (enriching the full ~19-candidate pool) is fixed - ratings enrichment is now capped to the top 10 candidates *after* scoring/ranking (`MAX_ENRICHED_BLEND_RESULTS` in both `vibe-blend` routes), and the movie detail page's own rating now goes through `ratings.ts`'s shared cache (`getMovieRatings`) instead of calling OMDb directly, so a title already looked up via a grid/mood-search/blend costs nothing extra there. `blendTitles` still fires one TMDB keyword call per candidate (~20 per blend, parallelized, best-effort) - that's TMDB, not OMDb, and TMDB isn't the rate-limited API here, so it's left as-is.
- "Explain more" LLM elaboration quality verified against the real Anthropic API on 2026-07-10: for The Mandalorian and Grogu (matched via mood search on "epic space opera with a found family crew"), the deterministic "Matches Adventure, space opera." expanded to "Follow a lone bounty hunter across the galaxy as he becomes an unlikely guardian to a mysterious child in this thrilling space opera adventure. Their journey through dangerous worlds and impossible odds will keep you on the edge of your seat." - a genuine improvement, grounded in the title's actual premise (not invented detail). One Playwright gotcha hit while verifying: the button lives in a zero-height `grid-rows-[0fr]` panel until real CSS `:hover` expands it, so `click({force: true})` without hovering first silently no-ops - needed an actual `.hover()` immediately before `.click()`.
- The "why this match" deterministic wording verified against a real mood-search interpretation on 2026-07-10: "epic space opera with a found family crew" → Science Fiction/Adventure/epic/space opera/adventure/found family, and The Mandalorian and Grogu's card read "Matches Adventure, space opera." - a coherent, accurate subset. Results overall were genuinely on-theme (Dune, the Star Wars catalog, Guardians of the Galaxy, 2001: A Space Odyssey).
- ~~Where-to-watch hardcoded the `"US"` region~~ — resolved 2026-07-10, see the "Region dropdown for Where to Watch" entry above. Still no automatic detection (geo-IP, `Accept-Language`) and still only two regions (US/Brazil) - an explicit user choice, not a limitation anymore, but worth expanding the region list if usage outside those two shows up.
- Filters-compose-with-mood (see the entry above) was verified end-to-end for both movies and TV via driven browser sessions - "gritty crime drama" (→ Crime+Drama) plus a Netflix filter on the `/series` page correctly surfaced Netflix's own crime catalog (Breaking Bad, Better Call Saul, You, Narcos), chips unchanged, request payload confirmed `cachedInterpretation` + `watchProviderIds`/`watchRegion` with no LLM round trip. Still unverified: `discoverWithMoodFallback`'s genre-narrowing fallback step (used when a genre+keyword combination is too thin) doesn't distinguish "genre came from the LLM" from "genre came from the user's own filter override" - if a user selects two genres via the checkboxes while mood is active and the combined result set is thin, the fallback could silently narrow to just one of the user's two picks, the same way it already narrows an LLM-extracted genre pair. Deliberately left as-is (matches the existing "prefer showing something over a strict-but-empty result set" philosophy) but worth watching if it ever produces a confusing genre drop with an active user override. Blend intentionally still doesn't compose with any filter (genre/rating/streaming) - out of scope for this pass, which was specifically about mood search's filter-composability.
